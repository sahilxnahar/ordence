{
  "name": "ordence-site",
  "version": "1.0.0",
  "private": true,
  "description": "The Ordence marketing site. Static HTML, deployed to Cloudflare Workers as assets.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:worker": "node scripts/build.mjs",
    "deploy": "node scripts/build.mjs && npx wrangler deploy"
  }
}
